import { FC } from 'react';
import { CategoriesType } from '../../../shared';
declare const RenderLink: FC<{
    haveChildren: boolean;
    href: string;
    itemChildren: CategoriesType;
    classNames: any;
}>;
export default RenderLink;
